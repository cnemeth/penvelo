<% semantic_form_for([:admin, @race_category]) do |f| %>
  <%= f.error_messages %>
  <% f.inputs do %>

      <%= f.input :name, :label => 'Name' %>

  <% end %>
  <%= f.submit "Submit" %>
<% end %>

