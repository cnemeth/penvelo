<% semantic_form_for([:club, @post]) do |f| %>
  <%= f.error_messages %>
  <% f.inputs do %>

      <%= f.input :title, :label => 'Title' %>
      <%= f.input :entry, :label => 'Entry' %>
      <%= f.input :tags, :label => 'Tags' %>

  <% end %>
  <%= f.submit "Submit" %>
<% end %>

